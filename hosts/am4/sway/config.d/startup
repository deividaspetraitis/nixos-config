# Run bluetooth manager
exec blueman-applet

# Run synology drive
exec synology-drive

# Run 1password
exec 1password --silent

# Run Screen color temperature manager
exec gammastep-indicator

# Notification Daemon
exec swaync

# Launch programs at the startup
exec $term
exec $browser
exec slack
exec obsidian
